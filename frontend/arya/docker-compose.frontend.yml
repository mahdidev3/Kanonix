services:
  arya-frontend:
    build:
      context: .
      target: dev
    ports:
      - "5173:5173"
    env_file:
      - .env.development
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev -- --host 0.0.0.0
